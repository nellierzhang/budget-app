---
description: When a merchant rule is saved, cascade it to subsequent matching rows
globs: index.html
alwaysApply: false
---

# Rule Cascade on Save

When `saveRule(i)` is called in `index.html`, after saving the rule to `userRules` and localStorage, iterate through all `classifiedRows` at indices **greater than `i`** and apply the new category/spending_category to any row whose merchant name contains the rule key (same substring-match logic as `matchRule`). Skip duplicate rows.

Use `merchantRuleKey(row.business)` for the stored key and `merchantMatchesRuleKey` for matching (same normalization as `matchMerchantRule`).

```js
// After saving userRules[key]:
for (let j = 0; j < classifiedRows.length; j++) {
  const r = classifiedRows[j];
  if (r.isDuplicate) continue;
  if (!merchantMatchesRuleKey(r.business, key)) continue;
  r.isNew = false;
  r.isFromHistory = false;
  if (j > i) {
    r.category = row.category;
    r.spending_category = row.spending_category;
  }
}
```

Category/spending cascade only flows **downward** (rows after `i`). Review flags (`isNew`, `isFromHistory`) clear on **all** matching rows so saved rules stop showing “review” / “from history”.
